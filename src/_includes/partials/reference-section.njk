{# ========================================================= 
   🜏 TGK — Reference Section Partial (v2.1)
   Purpose:
   Renders end-of-scroll reading lists and scholarly citations.

   ✅ Expects front-matter object:
     references:
       title: "Further Study"
       intro: "Explore the roots of this teaching."
       readings:
         - { title: "The Nag Hammadi Library", desc: "A collection of Gnostic scriptures." }
       scholarly:
         - { author: "Elaine Pagels", year: 1979, work: "The Gnostic Gospels", pub: "Random House" }

   ⚙️ Styling:
   - Uses `.section-block.source-footer` wrapper
   - Symbolic bullets via `.list-emoji`
   - Intended to appear before the footer / quiz section

   🧩 Structure:
   - references.title → section heading
   - references.intro → optional intro paragraph
   - references.readings → “Recommended Reading” list
   - references.scholarly → “Scholarly Sources & Translations” list
   ========================================================= #}

{% if references %}
<section id="resources" class="section-block source-footer">
  <h2 class="section-heading">{{ references.title }}</h2>

  {% if references.intro %}
    <p class="intro">{{ references.intro }}</p>
  {% endif %}

  {# --- Recommended Reading --- #}
  {% if references.readings %}
    <h3 class="sub-heading">🔍 Recommended Reading</h3>
    <ul class="list-emoji">
      {% for item in references.readings %}
        <li>
          <strong>{{ item.title }}</strong>
          {% if item.desc %}&mdash; {{ item.desc }}{% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}

  {# --- Scholarly Sources & Translations --- #}
  {% if references.scholarly %}
    <h3 class="sub-heading">📖 Scholarly Sources &amp; Translations</h3>
    <ul class="list-emoji">
      {% for item in references.scholarly %}
        <li>
          {{ item.author }} ({{ item.year }}). 
          <em>{{ item.work }}</em>.
          {% if item.pub %} {{ item.pub }}.{% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}

  <p class="disclaimer">
    These sources are recommended for study and context —
    they represent diverse voices and are offered as entry points, not dogma.
  </p>
</section>
{% endif %}

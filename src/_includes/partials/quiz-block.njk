{# =======================================================
   Quiz Block
   Renders a quiz section with heading + container + data
   Requires `quizId` in front-matter
   ======================================================= #}

{% if quizId %}
<section 
  id="quiz"
  class="section-block quiz-block" 
  data-quiz-id="{{ quizId }}">
  
  <h2 class="section-heading">🧠 Quiz</h2>
  <div class="quiz-container"></div>
</section>
{% endif %}


---
layout: base.njk

siteTitle: "The Gnostic Key"
pillarTitle: "TGK Community"
gateTitle: "Community Threads"
seriesTitle: "Discussion Threads"
seasonTitle: null
episodeTitle: null
partTitle: null

title: "Discussion Threads"
description: "Topic-based spaces for structured community dialogue using the Steel Man principle."

glyph: "💬"
glyphRow: ["✦", "☉", "✦"]

bodyClass: "community"
accent: "community"
tier: "free"

breadcrumbs:
  - { title: "The Gnostic Key", url: "/" }
  - { title: "TGK Community", url: "/pillars/tgk-community/" }
  - { title: "Discussion Threads" }
---

<main class="main-content">
  <section class="content-container">

    <!-- INTRO -->
    <section class="section-block">
      <h1 class="section-heading">Discussion Threads</h1>
      <p class="lead">
        A structured space for community dialogue. Clear topics, disciplined contributions,
        and the Steel Man principle at the foundation of every reply.
      </p>
      <p>
        Instead of scattered comments beneath scrolls, all discussion lives here in
        topic-based threads. This keeps the scrolls pristine while providing a sovereign
        space for interpretation, debate, reflection, and collaborative inquiry.
      </p>
    </section>

    <!-- GRID -->
    <section class="section-block">
      <h2 class="section-heading">Community Discussion Spaces</h2>

      <div class="threads-grid">

        {% for t in threadsGrid %}
          {% set state = t.state or "default" %}

          <article class="gnostic-card {{ state }}">
            <a class="card-link" href="{{ t.href }}">

              <div class="card-body">
                <div class="card-glyph">{{ t.glyph }}</div>
                <h3 class="card-title">{{ t.title }}</h3>

                {% if t.desc %}
                  <p class="card-desc">{{ t.desc }}</p>
                {% endif %}
              </div>

              {% if state == "coming-soon" %}
                <div class="card-overlay">
                  <span class="banner">Coming Soon</span>
                </div>
              {% endif %}

              <div class="card-badge tier-free">Free</div>

            </a>
          </article>

        {% endfor %}

      </div>
    </section>

  </section>
</main>

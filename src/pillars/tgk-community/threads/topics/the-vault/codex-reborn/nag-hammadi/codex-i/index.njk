---
layout: base.njk
---

<main class="main-content">
  <section class="content-container">

    <section class="section-block align-centre">
      <h1 class="section-heading">Codex I</h1>

      <p class="lead">
        Discussion hub for Codex I of the Nag Hammadi Library.
      </p>

      <p>
        This codex contains foundational Gnostic texts dealing with revelation,
        resurrection, error, awakening, and return. Use this hub to enter
        codex-level synthesis or proceed to individual text discussions.
      </p>
    </section>

    <section class="section-block">
      <div class="portal-grid">
        {% for m in topicGrid %}
          <article class="gnostic-card {{ m.state }}">
            <a class="card-link" href="{{ m.href }}">
              <div class="card-body">
                <div class="card-glyph">{{ m.glyph }}</div>
                <h3 class="card-title">{{ m.title }}</h3>
                <p class="card-desc">{{ m.desc }}</p>
              </div>
              <div class="card-badge tier-free">Free</div>
            </a>
          </article>
        {% endfor %}
      </div>
    </section>

  </section>
</main>

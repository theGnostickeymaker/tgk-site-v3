---
layout: base.njk
---

<main class="main-content">
  <section class="content-container">

    <section class="section-block align-centre">
      <h1 class="section-heading">Codex II</h1>

      <p class="lead">
        Discussion hub for Codex II of the Nag Hammadi Library.
      </p>

      <p>
        Codex II is the most expansive and influential of the Nag Hammadi codices,
        containing foundational mythic, theological, and anthropological texts.
        Use this hub to explore codex-level synthesis or enter individual text
        discussion threads.
      </p>
    </section>

    <section class="section-block">
      <div class="portal-grid">
        {% for m in topicGrid %}
          <article class="gnostic-card {{ m.state }}">
            <a class="card-link" href="{{ m.href }}">
              <div class="card-body">
                <div class="card-glyph">{{ m.glyph }}</div>
                <h3 class="card-title">{{ m.title }}</h3>
                <p class="card-desc">{{ m.desc }}</p>
              </div>
              <div class="card-badge tier-free">Free</div>
            </a>
          </article>
        {% endfor %}
      </div>
    </section>

  </section>
</main>

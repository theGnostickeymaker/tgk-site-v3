---
layout: "layouts/community-topic.njk"

topicId: "steel-man-method"
title: "The Steel Man Method"
description: "A structured discussion space dedicated to mastering and applying the Steel Man principle within the TGK Community."

glyph: "⚯"
state: "active"

minWriteTier: "initiate"
minCreateTier: "adept"

breadcrumbs:
  - { title: "The Gnostic Key", url: "/" }
  - { title: "TGK Community", url: "/pillars/tgk-community/" }
  - { title: "Discussion Threads", url: "/pillars/tgk-community/threads/" }
  - { title: "Topics", url: "/pillars/tgk-community/threads/topics/" }
  - { title: "The Steel Man Method" }
---


<main class="main-content">
  <section class="content-container">

    <!-- INTRO -->
    <section class="section-block">
      <h1 class="section-heading">Discussion Topics</h1>
      <p class="lead">
        All community discussion begins here. Topics are organised spaces where
        readers, seekers, and contrarians can debate, interpret, and explore ideas
        with the Steel Man ethic at the core.
      </p>
      <p>
        Future scrolls will link directly to their corresponding topics, ensuring
        clean pages and a unified conversation centre for the entire TGK ecosystem.
      </p>
    </section>

    <!-- TOPIC GRID (Dynamic) -->
    <section class="section-block">
      <h2 class="section-heading">Active & Upcoming Topics</h2>

      <div class="community-grid">

        {% for t in collections.communityTopics %}
        <article class="community-card {{ t.state }}">

          <!-- Badge (top right) -->
          {% if t.state == "coming-soon" %}
            <div class="card-badge">Coming Soon</div>
          {% endif %}
          {% if t.state == "locked" %}
            <div class="card-badge locked">Locked</div>
          {% endif %}
          {% if t.minWriteTier == "adept" %}
            <div class="card-badge">Adept</div>
          {% endif %}

          <a href="{{ t.url }}" class="card-link">

            <!-- Glyph -->
            <div class="community-glyph">{{ t.glyph }}</div>

            <!-- Title -->
            <h3 class="community-card-title">{{ t.title }}</h3>

            <!-- Description -->
            <p class="community-card-desc">{{ t.description }}</p>

            <!-- Optional overlay for non-active states -->
            {% if t.state != "active" %}
              <div class="card-overlay">
                <div class="banner">
                  {% if t.state == "coming-soon" %} Coming Soon {% endif %}
                  {% if t.state == "locked" %} Locked {% endif %}
                </div>
              </div>
            {% endif %}

          </a>
        </article>
        {% endfor %}

      </div>
    </section>

  </section>
</main>

---
layout: base.njk
title: "TGK Shop"
description: "Sacred artefacts and limited editions from The Gnostic Key."
tagline: "Artefacts ✦ cards ✦ sigils ✦ editions"

glyph: "✶"
accent: shop
bodyClass: shop

breadcrumbs:
  - { title: "The Gnostic Key", url: "/" }
  - { title: "<SET PILLAR NAME>", url: "<SET PILLAR URL>" }

---

{% include "partials/head-meta.njk" %}

<main class="main-content">
  <section class="page-title-block">
    <p class="page-title"><!-- Optional intro one-liner for this pillar --></p>
  </section>

  {% include "partials/pillar-grid.njk" %}

  <div class="gnostic-divider">
    <span class="divider-symbol pillar-glyph spin glow" aria-hidden="true">{{ pillarGlyph }}</span>
  </div>
</main>

{# JSON-LD #}
{% set items = pillarGrid %}{% set headline = title + " — Index" %}{% set base = page.url %}
{% include "partials/jsonld-collection.njk" %}

